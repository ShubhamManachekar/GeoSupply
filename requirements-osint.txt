# GeoSupply AI — OSINT Dashboard (lightweight runtime)
# Everything needed to run the REST API + live OSINT dashboard.
# No ML / vector-DB / graph libraries — those are only for the full swarm
# (see requirements.txt or `pip install -e ".[full]"`).
#
#   pip install -r requirements-osint.txt
#   python -m uvicorn geosupply.api.main:app --port 8000   (run from src/ on PYTHONPATH)

pydantic>=2.0,<3.0
httpx>=0.25.0
defusedxml>=0.7.1
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
structlog>=23.0.0
tenacity>=8.2.0
rich>=13.0.0
colorama>=0.4.6
python-dotenv>=1.0.0
anthropic>=0.40.0
mcp>=1.0.0
pyotp>=2.9.0
pyjwt>=2.8.0
cryptography>=41.0.0
