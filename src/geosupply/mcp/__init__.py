"""
GeoSupply MCP Package — Model Context Protocol integration.

Exposes the GeoSupply swarm as a FastMCP server so any MCP client
(Claude Code, Claude Desktop, custom agents) can call the swarm directly.

Entry point: geosupply-mcp  →  geosupply.mcp.server:main
"""
